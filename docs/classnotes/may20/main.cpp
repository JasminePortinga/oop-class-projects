#include <iostream>
#include <iomanip>
#include <string>
#include <fstream>
#include <sstream>
#include <array>
#include <cstdlib> // includes f'n prototype for rand

using namespace std;

int main() {
	srand(static_cast<unsigned int>(time(0))); //generate random number based on computer timer

	// string fileName{""}

	// each time you use "getline()", it skips one line. neat!

	// when analyzing data, check answer using pivot tables in excel

	// make sure you always close your files at the end. it's not gonna generate your files, and it leaves you vulnerable to hackers as well

	return 0;
}